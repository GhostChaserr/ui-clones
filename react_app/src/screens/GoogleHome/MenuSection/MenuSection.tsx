import {
  StyledAppMenuSection,
} from "../GoogleHomeStyles"

const AppMenuSection = ({ children }: any) => {
  return (
    <StyledAppMenuSection>
      {children}
    </StyledAppMenuSection>
  )
}

export default AppMenuSection